@model Appraisr.ViewModels.OrderViewModels.OrdersEditViewModel

@{
    ViewBag.Title = "Edit Order";
}

<h2>@ViewBag.Title</h2>

@using (Html.BeginForm())
{
    //@Html.ValidationSummary("The following errors were found:", new { @class = "alert alert-danger" })

    <h3>Order Information</h3>

    @Html.Partial("_OrderForm")

    <div class="row">
        <div class="col-md-12">
            <div class="pad-top">
                <button type="submit" class="btn btn-primary btn-lg margin-right">
                    <span class="glyphicon glyphicon-save"></span> Save
                </button>
                <a href="@Url.Action("Detail", new { id = Model.Order.Id })" class="btn btn-default btn-lg">
                    <span class="glyphicon glyphicon-remove"></span> Cancel
                </a>
            </div>
        </div>
    </div>
}